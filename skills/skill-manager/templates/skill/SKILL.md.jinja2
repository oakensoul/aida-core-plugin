---
type: skill
name: {{ name }}
description: {{ description }}
version: {{ version }}
tags:
{% for tag in tags %}
  - {{ tag }}
{% endfor %}
---

{{ spdx_md }}
# {{ name | replace("-", " ") | title }}

{{ description }}

## Activation

This skill activates when:

- Trigger condition 1
- Trigger condition 2

## Operations

### Operation 1

Description of operation 1.

**Process:**
1. Step 1
2. Step 2
3. Step 3

### Operation 2

Description of operation 2.

## Path Resolution

**Base Directory:** Provided when skill loads via `<command-message>` tags.

**Script Execution:** Construct full paths from base directory:

```text
{base_directory}/scripts/script-name{{ script_extension | default('.py') }}
```

**Reference Loading:** Reference files are located in `references/` subdirectory:

```text
{base_directory}/references/reference-name.md
```

## Resources

### scripts/

Executable scripts for this skill:

- **script-name.py** - Description
{% if use_aida_bootstrap | default(false) %}

> **AIDA Bootstrap:** Scripts in this skill use the AIDA managed
> virtual environment for Python dependencies. Add this to the top
> of each entry-point script (after stdlib imports):
>
> ```python
> import _paths  # noqa: F401  (sets up sys.path + bootstrap)
> ```
>
> Create a `_paths.py` in the `scripts/` directory following the
> pattern in other AIDA skills. This ensures third-party packages
> (jinja2, PyYAML, jsonschema, etc.) are available without requiring
> users to manually install them.
{% endif %}

### references/

Detailed workflow guides:

- **reference-name.md** - Description

---

*Created: {{ timestamp }}*
